```shell
ffmpeg -version
ffmpeg -devices    
ffmpeg -protocols
ffmpeg -filters
ffmpeg -muxers     
ffmpeg -demuxers
ffmpeg -decoders   
ffmpeg -encoders
ffmpeg -pix_fmts   
ffmpeg -sample_fmts
ffmpeg -layouts
```
